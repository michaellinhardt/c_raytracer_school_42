#include "raystruct.h"

void			menu_scene_add_btn_ok(void *gen, void *mlx)
{
	t_mlx	*m;

	m = mlx;
	ft_printf("scene add btn ok\n");
	// if (!m->menu.new_scene || !ft_strcmp(m->menu.new_scene
	// , TXT_SCENE_ADD_NAME_DEFAUT) || ft_strlen(m->menu.new_scene) < 4)
	(void)m;
	(void)gen;
	(void)mlx;
}
